mixin news-preview-card(title, desc, date, number)
  .news-preview-card(class=`news-preview-card__card-${number}`)
    a.news-preview-card__title(href="/post.html")= title
    p.news-preview-card__desc= desc
    span.news-preview-card__date= date